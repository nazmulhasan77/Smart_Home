{
  "rules": {
    "users": {
      "$uid": {
        // Strict data isolation: only the owner can read/write their device data
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
